<?xml version="1.0" encoding="utf-8"?>
<!--
  Allows plaintext HTTP only to the local dev backend (10.0.2.2 is the
  Android emulator's alias for the host machine's localhost). All other
  destinations, including any production API domain, still require HTTPS —
  App Store/Play Store review both flag apps that broadly disable ATS/
  cleartext-traffic protection.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>
    <base-config cleartextTrafficPermitted="false" />
</network-security-config>
